packages:
  - "packages/*"
# pnpm 10 ignored sharp's install script and both the website build and its
# image optimization were fine with the prebuilt @img/sharp-* binaries, so
# keep it off rather than start running it now.
allowBuilds:
  sharp: false
